# Dependencies — reinstalled from package-lock.json, never committed
node_modules/

# Build output — regenerated by `npm run build`
dist/

# Local env files — may hold secrets (e.g. EmailJS keys)
.env
.env.local
.env.*.local

# Editor / OS junk
.vscode/
.idea/
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
*.log
